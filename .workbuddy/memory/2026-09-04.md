# 2026-09-04

- 为 Cassette iOS App 设计了离线歌单操作界面稿（Ardot fileId 722160060034089），包含：离线歌单列表、歌单详情、批量选择歌曲三个主要屏幕。重点优化了离线状态展示、存储用量、筛选标签、一键下载/离线播放/批量操作等离线场景。

- 给 Cassette 播放器增加了「随机/列表/单曲」三态切换（PlaybackMode 枚举：list/single/shuffle）。新增 `Models/Domain/PlaybackMode.swift`；`PlayerState.playbackMode` 由 `repeatMode`+`isShuffled` 派生；`PlayerService.setPlaybackMode(_:)` 同步二者并维护队列乱序/还原；四处播放控件（FullPlayerExpandedView、BottomPlayerBar、FullPlayerView.queuePills、QueueView）把原来的「shuffle + repeat」两个按钮合并为一个三态循环按钮；`PlaybackSession`/`SessionPayload`/`RestoredSession` 持久化 `playbackModeRaw`（可选列，旧会话回退到 repeatMode 推导）。
- 修复上述改动引入的编译错误：`PlayerService.setPlaybackMode` 中三处 `MainActor.run({ ... })` 因闭包被解析为 `resultType:` 位置参数而报错（Missing argument label 'resultType:' / Closure passed to Bool.Type / Missing argument for 'body'），改为 `MainActor.run(body: { ... })`；同时给 `CassetteTests/LyricsViewModelTests.swift` 的 `MockPlayerService` 补上 `setPlaybackMode(_:)` 方法。
- 使用命令行 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -scheme Cassette -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17' -disableAutomaticPackageResolution build` 成功完成 iOS Simulator 构建验证（**BUILD SUCCEEDED**）。注意：默认的 macOS destination 会命中 `SongsListView.swift:158` 的 `.insetGrouped` 不可用错误，这是 macOS target 的既有问题，非本次改动引入。
- 根据截图反馈，iOS 主播放界面原本看不到三态按钮（按钮只放在队列界面）。遂在 `FullPlayerView.swift` 的 `PlaybackControlsView` 中、上一首按钮左侧新增播放模式切换入口（仅 iOS），点击循环切换 列表 → 单曲 → 随机；并给 `PlaybackMode` 增加 `accessibilityLabel: String` 避免 `LocalizedStringKey` 插值警告。iOS Simulator 构建验证通过。
